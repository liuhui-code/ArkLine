# ArkLine Development Log

- 2026-06-21: Replaced the old single-run terminal panel with an IDEA-style PTY-backed terminal tool window: added tabbed terminal sessions, frontend xterm-based viewport/input wiring, Tauri session lifecycle and output streaming, removed the obsolete terminal-panel path, and reran the full 77-test frontend suite plus the Rust terminal regression.
- 2026-06-21: Reworked ArkLine's semantic query path into a dual-provider shape: added deterministic frontend fallback symbol/completion helpers, implemented same-file Rust fallback definition/completion/document-symbols/usages behavior, introduced ArkTS provider discovery with truthful fallback health messaging, and surfaced the active semantic mode in the status bar with passing frontend and Rust regression coverage.
- 2026-06-21: Replaced the previous generated app icon work with a hand-authored final `Twin Pillar` SVG mark, then regenerated the full Tauri icon set so the project now ships a strict white-background, black-symbol icon across `png` and `icns` assets.
- 2026-06-21: Replaced the ArkLine app icon source with a transparency-safe variant and regenerated the full Tauri icon set so the rounded tile no longer picks up the previous dark corner matte.
- 2026-06-21: Fixed the first ArkLine application-icon integration gap by declaring the generated Tauri desktop icon set explicitly in `tauri.conf.json` and adding a macOS-only startup helper that sets the Dock icon during `tauri dev`, because Tao does not support runtime window icons on macOS.
- 2026-06-21: Landed the approved `C1` ArkLine application icon into the repository, saved the high-fidelity source under `docs/assets/`, and generated the full Tauri icon set (`png`/`ico`/`icns`/store assets) so the app and Windows installer can share the same branded identity.
- 2026-06-18: Bootstrapped the Tauri v2 workspace skeleton with frontend and Rust smoke tests.
- 2026-06-18: Added the first IDEA-inspired application shell with collapsible panes, shared design tokens, and shell tests.
- 2026-06-19: Added workspace and document state stores plus Rust workspace scanning and the first open_workspace command with tests.
- 2026-06-19: Added document conflict handling, fuzzy path ranking, search/problem/diff stores, Quick Open, keyboard save flow, and expanded frontend MVP tests.
- 2026-06-19: Wired save, lint/format diagnostics, and diff review into the shell, plus Tauri commands for document save, validation, and workspace diff loading.
- 2026-06-19: Integrated CodeMirror 6 as a lazy-loaded editor foundation, added a settings dialog and settings store, and introduced a backend environment doctor plus atomic settings storage tests.
- 2026-06-19: Connected settings load/save through Tauri commands, removed unnecessary settings hydration churn in the shell, and recorded the split editor bundle approach to keep the initial frontend payload smaller.
- 2026-06-19: Expanded the settings surface with lint/format command fields, made environment doctor report configured tool commands, and updated README/notices to reflect the current MVP state and Windows packaging flow.
- 2026-06-19: Fixed the Tauri Windows bundle config for CLI 2.0.0 by moving `minimumWebview2Version` under `bundle.windows.nsis`, and downgraded Vite to 5.4.21 so `pnpm tauri dev` works on the current Node 21 development machine.
- 2026-06-19: Finalized the IDEA New UI-inspired shell design for the next MVP phase, including Project tree density, unified bottom tool windows, Git-owned diff flow, and IDEA-style keyboard exit rules.
- 2026-06-19: Wrote the shell implementation plan for the IDEA-inspired redesign, including component splits, bottom tool-window refactor, Git-owned diff flow, hotkey rules, and ArkTS demo validation.
- 2026-06-19: Split the shell into focused top bar, left tool rail, overlay, and shell state modules, and added landmark coverage for the approved shell regions in frontend tests.
- 2026-06-19: Reworked the bottom Git experience into a single IDEA-like tool window with Local Changes and an embedded diff viewer, and updated shell regression tests to assert the Git-owned review flow.
- 2026-06-19: Added IDEA-style shell hotkeys and exit behavior, unified query overlays for Quick Open and related lookup flows, and validated the MVP against an ArkTS-shaped sample workspace journey.
- 2026-06-19: Added the terminal MVP with preset validation actions, manual command execution, history, rerun, clear, stop, and a Tauri-backed single-run command service; also split shell rendering further so AppShell stays under the 500-line limit.
- 2026-06-19: Refreshed the shell chrome toward IntelliJ IDEA New UI with denser desktop spacing, weaker toolbar buttons, clearer pane layering, lighter dividers, calmer status styling, and a more editor-first visual hierarchy.
- 2026-06-19: Tightened the editor-adjacent IDEA polish by adding Project/Workspace pane captions, split status-bar groups, tab dirty-dot treatment, and denser tree/search/tool-window styling while keeping AppShell within the 500-line guardrail.
- 2026-06-19: Captured and approved the high-fidelity `A · Strict IDEA New UI` shell mockup at `docs/superpowers/specs/2026-06-19-arkline-idea-reference-approved.html`, and froze it as the sole visual baseline for subsequent shell replication work.
- 2026-06-19: Implemented the first strict replication pass from the approved `A` mockup: top toolbar button groups, icon-only left activity rail, denser IDEA-like Project tree rows with caret/file markers, updated shell tests, and restored `AppShell.tsx` to the 500-line limit.
- 2026-06-19: Implemented the second strict replication pass from the approved `A` mockup: placeholder editor welcome tab treatment, tighter editor/bottom tab framing, Git change status chips and viewer metadata, and updated regression coverage while keeping the frontend suite fully green.
- 2026-06-19: Refined the bottom Terminal tool window toward the frozen `A` mockup with IDEA-like action buttons, denser output framing, command-row styling, and focused regression coverage for the terminal shell interactions.
- 2026-06-19: Tightened the Project, Git, and status-bar shell chrome toward the frozen `A` mockup by adding pane-header action dots, denser bottom tab framing, Git sidebar/viewer hierarchy, subdued selected-row treatment, and status-bar emphasis coverage.
- 2026-06-19: Realigned the top bar and editor tabs to the frozen `A` mockup by restoring the approved toolbar grouping (Search and Quick Open on the left, Git and Terminal on the right), wiring those buttons onto existing shell actions, tightening tab chrome, and expanding shell regression coverage for the updated interactions.
- 2026-06-19: Refined the editor surface toward the frozen `A` mockup by turning the empty state into a weaker centered welcome view, tightening editor-area rhythm, and extending shell coverage for the new welcome-state structure.
- 2026-06-19: Tightened the editor core against the frozen `A` mockup by adding explicit empty/active editor-surface states and refining the CodeMirror theme for subtler gutters, calmer selections, and IDEA-like content spacing.
- 2026-06-19: Stabilized the IDEA-style shell hotkey coverage by making the lazy editor test flow wait for the real editor surface before firing keyboard shortcuts, then reran the full frontend suite to confirm all 43 tests pass.
- 2026-06-20: Completed the next strict IDEA-shell replication pass by turning the top `File/Edit/View` buttons into working menus, wiring a real ArkTS formatting action into the toolbar and save flow, tightening topbar button chrome with icon treatment, and extending the shell regression suite to 45 passing tests while keeping `AppShell.tsx` under the 500-line limit.
- 2026-06-20: Tightened the frozen `A` mockup replication further by moving the shell palette back to IDEA-like neutral grays, weakening pane and status emphasis, restyling the terminal chrome, and restructuring Git diff rows with line-number gutters and added/removed backgrounds while keeping the full 45-test frontend suite green.
- 2026-06-20: Fixed the top-shell project actions by replacing the fake demo-only open button with a real project-path dialog, adding a recent-projects flow, removing the incorrect recent-files wiring from the File menu, and keeping the shell regression suite green while bringing `AppShell.tsx` back under the 500-line limit.
- 2026-06-20: Brought the File menu closer to IDEA by moving project opening out of the toolbar, wiring Tauri's native directory picker through the dialog plugin with a typed-path fallback dialog, persisting recent projects in settings, removing the redundant top-level Quick Open/Open buttons, and verifying the frontend build, Rust tests, and full 47-test frontend suite.
- 2026-06-20: Hardened project opening so native folder selection no longer fails silently: desktop cancel now does nothing, workspace-open failures surface through the status bar and fallback dialog, and the shell/build/test suite was rerun with the regression count extended to 48 frontend tests.
- 2026-06-20: Fixed cross-platform workspace opening by removing Windows-only path normalization from the Tauri dialog flow, unifying frontend path helpers and project-tree/status basename rendering, returning native paths from the Rust workspace scanner, and rerunning `pnpm build`, the full 48-test frontend suite, and all 13 Rust tests.
- 2026-06-20: Fixed Unix absolute-path selection in the Project tool window by preserving the leading `/` when rebuilding tree entry paths, and added regression coverage so clicked files highlight correctly after opening a macOS workspace.
- 2026-06-20: Fixed editor cursor loss by keeping the CodeMirror view alive across controlled value updates instead of recreating it on every keystroke, and added a regression test that asserts the editor instance stays stable while typing.
- 2026-06-20: Added `docs/editor-capability-matrix.md` to turn IDEA-style editing and code-query expectations into a tracked acceptance matrix, separating implemented editor-core behavior from missing language-service-backed IDE features and defining the next implementation order.
- 2026-06-20: Added editor-core regression coverage for selection replacement, undo/redo, and recent-files query ordering, and fixed the Recent Files overlay to read from the tracked most-recent-first file history instead of tab order.
- 2026-06-20: Added the next editor/query regression batch for caret-movement typing behavior and Search Everywhere open-file flow, including overlay-close verification after opening a result from the toolbar search surface.
- 2026-06-20: Added the next query/navigation slice with Recent Projects reopening, Search Everywhere result focus return, and a minimal Command Palette -> Go to Line flow; also extracted AppShell helper modules so `AppShell.tsx` stays under the 500-line limit while the shell regression suite and frontend build remain green.
- 2026-06-20: Switched CodeMirror selection rendering back to the browser-native path by removing `drawSelection()`, after isolating the large-area selection issue to desktop selection rendering rather than selection-state creation; verified the focused editor and shell suites plus frontend build remain green.
- 2026-06-20: Added the ArkTS language-service adapter skeleton across Rust and TypeScript, including backend command/model/service boundaries, a mock semantic path for non-Tauri demo workspaces, and focused regression coverage for capability inspection, hover, definition, and completion wiring without claiming a real provider is connected yet.
- 2026-06-20: Wired the first UI consumer of the ArkTS language-service skeleton by adding editor caret tracking, `Ctrl+B` / command-palette `Go to Definition` handling, normalized same-file definition routing, and regression coverage that verifies the shell issues a definition query and reflects the target in the status bar.
- 2026-06-20: Added the first completion MVP on top of the ArkTS language-service skeleton: `Ctrl+Space` and command-palette `Code Completion` now query completion items, show a filtered completion overlay, and insert the chosen item back into the editor; also re-compacted `AppShell.tsx` to stay under the 500-line guardrail while keeping shell hotkey, language-service, and build verification green.
- 2026-06-20: Completed the first ArkTS query loop MVP by adding `Alt+F7` usages search with a persistent bottom `Usages` tool window, result click-through navigation into the editor, and `Ctrl+Click` definition triggering from CodeMirror; verified with focused shell/editor regression suites while keeping `AppShell.tsx` at 435 lines.
- 2026-06-20: Fixed the first real-world definition-click gap by changing `Ctrl+Click` to resolve the clicked editor position instead of the stale caret, and added a same-file local-definition fallback for cases where the Tauri language-service adapter still returns no definition target; covered the fallback parser directly and reran the focused shell/editor suites.
- 2026-06-20: Rewrote the README for the first public snapshot so GitHub readers can understand the MVP scope and quickly run ArkLine on Windows or validate it on macOS before the first repository push.
- 2026-06-20: Tightened first-push repository hygiene by ignoring TypeScript build-info cache files before creating the initial GitHub snapshot.
- 2026-06-20: Tightened first-push repository hygiene again by excluding local `.superpowers/` brainstorming artifacts from source control while keeping the approved design/spec documents under `docs/superpowers/`.
- 2026-06-20: Reshaped the README into a GitHub-ready project homepage with clearer positioning, first-use flow, shortcut summary, architecture snapshot, repository map, and near-term roadmap for external readers.
- 2026-06-20: Added first-release publication materials, including a `v0.1.0` release draft and a concrete screenshot capture checklist so the repository can move from code snapshot to publishable project presentation.
- 2026-06-20: Added a Windows preflight-and-build PowerShell helper that checks Node, pnpm, Rust, MSVC tooling, and WebView2 presence before running the ArkLine Windows build flow, and documented its usage in the README.
